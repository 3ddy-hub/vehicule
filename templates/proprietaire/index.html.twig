{% extends 'base.html.twig' %}

{% block title %}{{ 'bo.propritaries'|trans }}{% endblock %}

{% set proprietaire_link = true %}

{% block breadcrumb %}
    {% include 'partials/breadcrumb.html.twig' with {
        'title' : 'bo.propritaries'|trans,
    } %}
{% endblock %}
{% block container %}
    <section class="content">
        <div class="container-fluid">
            {% for message in app.flashes('success') %}
                <div class="alert alert-success">
                    {{ message }}
                </div>
            {% endfor %}
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-header">
                            <a href="{{ path('proprietaire_new') }}" class="btn btn-outline-primary btn-sm"><i
                                        class="fa fa-plus"></i> {{ 'bo.add'|trans }}</a>
                        </div>
                        <div class="card-body">
                            <table id="proprietary-list" class="table table-bordered table-striped">
                                <thead>
                                <tr>
                                    <th>{{ 'bo.brand'|trans }}</th>
                                    <th>{{ 'bo.lastname'|trans }}</th>
                                    <th>{{ 'bo.firstname'|trans }}</th>
                                    <th>{{ 'bo.address'|trans }}</th>
                                    <th>{{ 'bo.postal.code'|trans }}</th>
                                    <th>{{ 'bo.tel'|trans }}</th>
                                    <th>{{ 'bo.action'|trans }}</th>
                                </tr>
                                </thead>
                                <tbody></tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}
{% block javascripts %}
    {{ parent() }}
    <script>
        // url ajax
        let url_proprietary_ajax_list = "{{ path('voiture_ajax_list') }}";
        // btn ud
        let edit_path = "{{ path('proprietaire_edit',{'id':0}) }}"
        let delete_path = "{{ path('proprietaire_delete',{'id':0}) }}"
    </script>
    <script src="{{ asset('js/proprietary.ajax.list.js') }}"></script>
{% endblock %}
